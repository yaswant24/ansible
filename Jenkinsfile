pipeline {
  agent any
  stages {
    stage('build') {
      steps {
        sh 'echo " this is build stage"'
      }
    }

    stage('deploy') {
      steps {
        sh 'echo " this is deploy phase"'
      }
    }

  }
}